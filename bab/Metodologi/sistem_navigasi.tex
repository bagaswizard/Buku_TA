
\begin{figure}[H]
\centering
\includesvg[width=0.7\textwidth]{gambar/navigation_architecture.svg}
\caption{Arsitektur sistem navigasi}
\label{fig:arsitektur_navigasi}
\end{figure}

Sistem navigasi ini berfungsi untuk merencanakan dan mengeksekusi
pergerakan robot dari satu region ke region lain pada lingkungan yang
telah diketahui. Sistem dapat dibagi menjadi empat komponen utama,
yaitu occupancy grid, costmap, lokalisasi, serta path planning dan
tracking. Occupancy grid menyediakan representasi diskrit dari
lingkungan. Costmap menyimpan informasi tersebut dalam bentuk peta
biaya (costmap). Lokalisasi bertugas melacak posisi dan region robot
secara real-time. Path planning dan tracking bertanggung jawab
menghasilkan jalur antar region dan menjalankannya. 

Data combined map (peta gabungan) digunakan untuk membangun global
costmap, dimulai dari pembentukan static layer yang merepresentasikan
obstacle yang diketahui dari peta. Selain itu, combined map juga
menjadi basis bagi lokalisasi ICP untuk dikomparasi dengan hasil scan
LiDAR guna memperkirakan pose robot. Sementara itu, map transisi
digunakan sebagai sumber data bagi transition costmap, yang menyimpan
informasi koneksi antar region.

Costmap dalam sistem ini dibagi menjadi tiga lapisan, yaitu global
costmap, local costmap, dan transition costmap. Global dan local
costmap sama-sama menggunakan inflation layer, namun dengan sumber
data yang berbeda. Inflation layer pada global costmap dibangun
berdasar-kan static layer yang berasal dari obstacle tetap pada peta.
Inflasi pada local costmap didasarkan pada obstacle layer yang
diperbarui secara dinamis dari data scan LiDAR.

Global costmap digunakan oleh global planner untuk merencanakan path
utama menuju tujuan. Untuk navigasi antar region, path router bertugas merutekan ulang jalur menuju
zona transisi ketika tujuan berada di region yang berbeda. Hasil
rutekan tersebut kemudian diteruskan ke DWA local planner, yang
menghasilkan trajectory berupa kecepatan x, y, dan rotasi yang
diberikan ke kontroler robot untuk dieksekusi.

Untuk memfasilitasi tracking, lokalisasi ICP dan region switcher bekerja sama melacak posisi koordinat
robot serta region tempat robot berada saat ini. Ketika robot mencapai
zona transisi, region switcher melakukan perpindahan region dan
memperbarui posisi awal ICP pada region yang baru.

\begin{figure}[H]
\centering
\includesvg[width=0.5\textwidth]{gambar/main_nav_flow.svg}
\caption{Diagram alur navigasi}
\label{fig:main_navigation_flow}
\end{figure}


\subsection{Lokalisasi Iterative Closest Point (ICP)}

Sistem navigasi ini menggunakan algoritma Iterative Closest Point (ICP) yang digunakan untuk memperkirakan koreksi pose robot terhadap static occupancy grid map. ICP bekerja dengan mencocokkan titik-titik dari laser scan dengan titik-titik yang sesuai pada peta, lalu menghitung transformasi optimal yang meminimalkan jarak antara pasangan titik tersebut. Proses ini iteratif, di mana setiap iterasi memperbaiki estimasi pose robot hingga konvergen ke solusi yang stabil. Sebelum ICP, dilakukan prekomputasi KNN untuk mempercepat pencarian pasangan titik selama proses ICP. 

\subsubsection{Prekomputasi KNN}

Sistem ini melakukan prekomputasi atas semua kemungkinan pasangan titik (sensor point → map point) saat menerima map baru, lalu menyimpan informasi tersebut di dalam hash table. Pendekatan ini dimungkinkan karena occupancy grid bersifat diskrit dan diketahui secara penuh. Setiap sel bebas di dekat tembok dapat diketahui sebelumnya K buah sel rintangan terdekatnya.
Proses ini disebut interpretasi map dan terjadi sekali saat startup, bukan saat runtime. Hasilnya lookup KNN saat runtime menjadi O(1).

Untuk setiap sel rintangan (occupied) pada indeks $i_{\text{occ}}$:
\begin{enumerate}
    \item Cek kejangkauan menggunakan Moore neighbourhood 3×3 (8 tetangga terdekat). Jika tidak ada satupun sel bebas di antara tetangganya, maka sel rintangan tersebut berada di dalam tembok (interior) dan tidak akan pernah terdeteksi oleh laser hanya mengenai permukaan tembok, bukan interiornya. Sel seperti ini diabaikan untuk menghemat memori dan komputasi.
    \item Iterasi area sekitar sel rintangan yang dapat dicapai. Semakin besar $r$, semakin banyak sel yang akan memiliki KNN precomputed, tetapi juga semakin besar ukuran hash table.
    \item Untuk setiap sel tetangga pada indeks $i_{\text{nb}}$ yang berada dalam radius $r$, \emph{world coordinates} dari sel rintangan $i_{\text{occ}}$ dimasukkan ke dalam daftar KNN milik $i_{\text{nb}}$. Daftar ini dijaga tetap terurut berdasarkan jarak Euclidean  dan dibatasi maksimal $K$ entry. Jika sudah penuh dan jarak lebih besar dari entry terjauh, data diabaikan, namun jika jarak lebih kecil, entry terjauh dihapus dan data baru dimasukkan pada posisi yang tepat.
\end{enumerate}

\subsubsection{Proses Data LiDAR}
Data scan dari sensor LiDAR diproses terlebih dahulu dan disimpan dalam format seperti berikut.
Untuk scan dengan sinar $N$ pada sudut $\theta_0, \theta_1, \dots, \theta_{N-1}$:

\begin{equation}\mathbf{C} = \begin{bmatrix} \cos\theta_0 & \cos\theta_1 & \cdots & \cos\theta_{N-1} \\ \sin\theta_0 & \sin\theta_1 & \cdots & \sin\theta_{N-1} \end{bmatrix}\end{equation}

Cache hanya dihitung ulang ketika parameter scan (jumlah sinar, rentang sudut, increment) berubah.

Diberikan pembacaan vektor $r_0, r_1, \dots, r_{N-1}$:

\begin{equation}\mathbf{S} = \begin{bmatrix} r_0 \cos\theta_0 & r_1 \cos\theta_1 & \cdots & r_{N-1} \cos\theta_{N-1} \\ r_0 \sin\theta_0 & r_1 \sin\theta_1 & \cdots & r_{N-1} \sin\theta_{N-1} \end{bmatrix}\end{equation}

$S$ adalah hasil \emph{scan point cloud} dalam koordinat cartesian, hasil konversi dari data laser mentah ke titik-titik 2D.

\subsubsection{Loop Utama}

\begin{algorithm}[H]
\caption{ICP Single Iteration}
\begin{algorithmic}[1]
\Require $scan$ \Comment{$N$ points dalam sensor frame}
\Ensure $new\_to\_old$ \Comment{transform\_t}
\State $T \gets \text{Identity}$
\For{$iteration = 1$ \textbf{to} $max\_iter$}
    \State $random\_scan \gets \text{sampler}(scan)$ \Comment{subsample dengan random stride}
    \State $data \gets T \cdot random\_scan$ \Comment{transform ke map frame}
    \State $M \gets \text{matches}(data, knn)$ \Comment{cari correspondences}
    \State $w \gets \text{get\_weights}(M)$ \Comment{weight setiap match}
    \State $T\_update \gets \text{point\_to\_map}(M, w)$ \Comment{SVD solver}
    \State $T \gets T\_update \cdot T$ \Comment{akumulasi}
    \If{$\text{converged}(T\_update)$}
        \State \textbf{break}
    \EndIf
\EndFor
\State \Return $T$
\end{algorithmic}
\end{algorithm}

% Kenapa ada transformasi di awal step: Karena KNN disimpan dalam koordinat frame map, bukan frame sensor.

\subsubsection{Correspondence Matching}

Correspondence matching adalah langkah yang memasangkan setiap titik dari laser scan dengan titik terdekat dari map. Proses ini dilakukan dengan memanfaatkan hash table KNN yang sudah diprekomputasi sebelumnya. Hasil akhirnya digunakanuntuk menghitung transformasi optimal melalui SVD solver.

Untuk setiap sensor point $s_j$:

\begin{enumerate}
  \item Cek apakah $s_j$ berada di dalam map bounds. Jika tidak, lewati $s_j$.
  \item melakukan lookup ke hash table untuk mendapatkan daftar hingga $K$ titik map terdekat yang mungkin menjadi pasangan
  \item Duplikasi $s_j$ untuk setiap nearest neighbors-nya, menghasilkan $(s_j, m_{j,1}), (s_j, m_{j,2}), \dots,\\ (s_j, m_{j,K})$. Ini memperluas $N$ sensor points menjadi hingga $N \times K$ pasangan yang cocok.
\end{enumerate}

\subsubsection{Solusi SVD}

Solusi SVD ini digunakan untuk menghitung transformasi yang memetakan titik-titik dari laser scan ke titik-titik yang sesuai di map, berdasarkan pasangan yang ditemukan pada langkah correspondence matching.

Diberikan $M$ pasangan $\{s_i\}$ (sensor) dan $\{m_i\}$ (map) dengan weights $\{w_i\}$, kita mencari rigid transform $T \in SE(2)$ yang meminimalkan transformasi. $SE(2)$ berarti transformasi yang mempertahankan jarak dan orientasi.

% Intinya: $T \in SE(2)$ berarti transformasi yang mempertahankan jarak dan orientasi (tidak ada shearing/scaling) — hanya memutar dan menggeser

\begin{equation}T^* = \arg\min_{R,\,t} \sum_{i=1}^{M} w_i \, \big\| m_i - (R s_i + t) \big\|^2\end{equation}

Langkah 1: Kalkulasi \emph{Weighted Centroids}

\begin{equation}\bar{s} = \frac{\sum_{i=1}^{M} w_i s_i}{\sum_{i=1}^{M} w_i}, \qquad \bar{m} = \frac{\sum_{i=1}^{M} w_i m_i}{\sum_{i=1}^{M} w_i}\end{equation}

Langkah 2: Centering (pindahkan titik ke pusat koordinat)

\begin{equation}s_i' = s_i - \bar{s}, \qquad m_i' = m_i - \bar{m}\end{equation}

Langkah 3: Cross-Covariance Matrix

\begin{equation}H = \sum_{i=1}^{M} w_i \, m_i' (s_i')^\top = M \, W \, S^\top\end{equation}

dimana $S, M \in \mathbb{R}^{2 \times M}$ adalah matriks yang kolomnya berisi titik-titik yang sudah di-center, dan $W = \operatorname{diag}(w_1, \dots, w_M)$.

Langkah 4: Singular Value Decomposition

\begin{equation}H = U \Sigma V^\top\end{equation}

dengan $U, V \in \mathbb{R}^{2 \times 2}$ orthonormal dan $\Sigma = \operatorname{diag}(\sigma_1, \sigma_2)$.

Langkah 5: Rotasi Optimal

\begin{equation}R = U V^\top\end{equation}

Jika $\det(R) < 0$ (menghasilkan refleksi, bukan rotasi murni), koreksi dengan membalik tanda baris pertama $V^\top$:

\begin{equation}\text{if } \det(R) < 0: \quad V^\top_{0,:} \gets -V^\top_{0,:}, \quad R = U V^\top\end{equation}

Langkah 6: Translasi Optimal

\begin{equation}t = \bar{m} - R \bar{s}\end{equation}

Langkah 7: Susun Rigid Transform 2D

\begin{equation}T_{\text{update}} = \begin{bmatrix} R & t \\ 0 & 1 \end{bmatrix} = \begin{bmatrix} \cos\Delta\theta & -\sin\Delta\theta & t_x \\ \sin\Delta\theta & \cos\Delta\theta & t_y \\ 0 & 0 & 1 \end{bmatrix}\end{equation}

Setelah iterasi, cek apakah transform update cukup kecil:

\begin{equation}\text{converged} \iff \|t\| < t_{\text{norm}} \land \big|\arctan2(R_{1,0}, R_{0,0})\big| < r_{\text{norm}}\end{equation}

% konvergensi terjadi hanya jika kedua kondisi terpenuhi, dan jika kedua kondisi terpenuhi maka konvergensi terjadi.

Untuk mempercepat, hanya $1/s$ dari scan points yang digunakan:
\begin{enumerate}
\item Pilih offset acak $o \in [0, s-1]$
\item Ambil setiap point ke-$s$ mulai dari $o$:
\end{enumerate}

\begin{equation}p_k' = p_{o + k \cdot s}, \quad k = 0, 1, \dots, \left\lfloor \frac{N}{s} \right\rfloor - 1\end{equation}

\begin{table}[H]
\centering
\caption{Parameter Default ICP}
\label{tab:icp_defaults}
\begin{tabular}{ccc}
\toprule
\textbf{Parameter} & \textbf{Nilai Default} & \textbf{Keterangan} \\
\midrule
$\text{max\_iter}$ & 50 & Iterasi maksimum ICP \\
$s$          & 2            & \emph{Random stride} subsampling \\
$t_{\text{norm}}$ & $0.01$ m & Batas konvergensi translasi \\
$r_{\text{norm}}$ & $0.01$ rad ($\approx 0.57^\circ$) & Batas konvergensi rotasi \\
$k$          & 7            & Jumlah \emph{nearest neighbors} \\
$r$          & 10           & Radius pencarian \emph{neighbor} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Costmap}

Costmap adalah representasi grid yang menyimpan informasi biaya untuk navigasi. Setiap sel dalam costmap memiliki nilai biaya yang mencerminkan kesulitan atau risiko melewati area tersebut. Nilai biaya ini dapat dipengaruhi oleh berbagai faktor seperti keberadaan rintangan. Costmap menyimpan informasi map grid 2D dalam bentuk array 1D 

\begin{table}[H]
\centering
\caption{Nilai konstanta costmap}
\label{tab:costmap_constants}
\begin{tabular}{lcc}
\toprule
\textbf{Nama Konstanta} & \textbf{Nilai} & \textbf{Makna} \\
\midrule
\texttt{NO\_INFORMATION}                & 255 & Tidak diketahui (\emph{unknown}) \\
\texttt{LETHAL\_OBSTACLE}               & 254 & Obstacle mematikan  \\
\texttt{INSCRIBED\_INFLATED\_OBSTACLE}  & 253 & Batas terluar inflasi obstacle \\
\texttt{INFLATED\_OBSTACLE}              & 6-252 & Inflasi obstacle \\
\texttt{TRANSITION\_CELL}                 & 1-5 & Area transisi antar region \\
\texttt{FREE\_SPACE}                    & 0   & Ruang bebas  \\
\bottomrule
\end{tabular}
\end{table}

Indeks Grid

konversi dari koordinat grid 2D $(m_x, m_y)$ ke flat index 1D:

\begin{equation}i = m_y \cdot W + m_x\end{equation}

dimana $W$ adalah lebar grid (jumlah cell per baris).

Koordinat Dunia - Grid

Diberikan origin grid di $(o_x, o_y)$ dan resolusi $r$ (meter per cell):

World → Grid:

\begin{equation}m_x = \left\lfloor \frac{w_x - o_x}{r} \right\rfloor, \qquad m_y = \left\lfloor \frac{w_y - o_y}{r} \right\rfloor\end{equation}

Grid → World :

\begin{equation}w_x = o_x + \left(m_x + \frac{1}{2}\right) \cdot r, \qquad w_y = o_y + \left(m_y + \frac{1}{2}\right) \cdot r\end{equation}

1.4 Rolling Window

Untuk local costmap yang bergerak mengikuti robot, origin grid digeser:

\begin{equation}\Delta_x = \lfloor \text{new\_origin}_x - \text{old\_origin}_x \rfloor, \quad \Delta_y = \lfloor \text{new\_origin}_y - \text{old\_origin}_y \rfloor\end{equation}

Array costmap digeser sebesar $(\Delta_x, \Delta_y)$ dan cell baru diisi dengan NO\_INFORMATION.

% 1. Jika rolling window, geser origin ke pusat robot
% 2. Reset bounds: min_x = max_x = robot_x, min_y = max_y = robot_y
% 3. Untuk setiap layer (urut):
%      layer->updateBounds(robot_x, robot_y, robot_yaw, &min_x, &min_y, &max_x, &max_y)
%      → perluas bounds sesuai kebutuhan layer
% 4. Konversi bounds ke cell coordinates:
%      worldToMapEnforceBounds(min_x, min_y, &cell_min_x, &cell_min_y)
%      worldToMapEnforceBounds(max_x, max_y, &cell_max_x, &cell_max_y)
% 5. Reset region [cell_min..cell_max] ke nilai default
% 6. Untuk setiap layer (urut):
%      layer->updateCosts(master, cell_min_x, cell_min_y, cell_max_x, cell_max_y)
%      → layer menulis cost ke master



\subsubsection{Static Layer}
% \label{staticlayer}
Static layer adalah lapisan costmap paling dasar yang menyimpan informasi \emph{known obstacle} atau tembok yang sudah diketahui. Lapisan costmap ini mengkonversi setiap sel dari nilai occupancy grid (0-100) ke nilai biaya costmap internal (0-255). Sel yang tidak diketahui (unknown) diberi biaya tertinggi, sel bebas diberi biaya terendah, dan sel yang terisi (occupied) diberi biaya tinggi. 

\begin{equation}C_{\text{internal}} = \begin{cases} 255 & \text{if } OG = -1 \text{ (unknown)} \\ 0 & \text{if } OG = 0 \text{ (free)} \\ 254 & \text{if } OG \geq 100 \text{ (occupied)} \\  \end{cases}\end{equation}

\subsubsection{Obstacle Layer}
Obstacle layer dirancang untuk menangani objek-objek dinamis atau rintangan yang baru terdeteksi di lingkungan. Informasi pada lapisan ini diperoleh secara real-time dari sensor LiDAR robot, yang terus-menerus memindai area sekitar dan memperbarui \emph{costmap} sesuai dengan obstacle yang terdeteksi. Lapisan ini memiliki batasan jangkauan observasi maksimum, yang jaraknya disesuaikan berdasarkan jarak \emph{pointcloud} maksimum dari sensor LiDAR, yang berarti hanya rintangan dalam radius pandang sensor yang akan direpresentasikan di lapisan \emph{costmap} ini.

Untuk setiap beam laser dengan range $r$ dan sudut $\theta$:

\begin{enumerate}
  \item Hitung endpoint dalam frame sensor: $(r\cos\theta,\; r\sin\theta)$
  \item Transform ke frame map via TF
  \item Konversi ke cell: $(m_x, m_y) = \text{worldToMap}(x, y)$
  \item Set cell ke \texttt{LETHAL\_OBSTACLE} (254)
\end{enumerate}

Gunakan algoritma Bresenham 2D untuk menggambar garis dari robot ke endpoint sensor. Semua cell yang dilewati garis (sebelum endpoint) di-clear:

\begin{equation}C_{\text{master}}(m_x, m_y) = 0 \quad \forall \text{cell di sepanjang ray} \text{ (kecuali endpoint)}\end{equation}

Bresenham 2D Raytracing

Algoritma Bresenham memilih pixel-pixel yang mendekati garis lurus antara dua titik grid. Untuk setiap langkah, algoritma memutuskan apakah akan bergerak di sumbu x, y, atau keduanya berdasarkan error accumulator:

Diberikan titik awal $(x_0, y_0)$ dan titik akhir $(x_1, y_1)$:

\begin{equation}\Delta_x = |x_1 - x_0|, \quad \Delta_y = |y_1 - y_0|\end{equation}

\begin{equation}\text{err} = \Delta_x - \Delta_y, \quad s_x = \text{sign}(x_1 - x_0), \quad s_y = \text{sign}(y_1 - y_0)\end{equation}

\begin{algorithm}[H]
\caption{Bresenham Raytracing}
\begin{algorithmic}[1]
\While{$x \neq x_1 \lor y \neq y_1$}
    \State $e_2 \gets 2 \cdot \mathit{err}$
    \If{$e_2 > -\Delta y$}
        \State $\mathit{err} \gets \mathit{err} - \Delta y$
        \State $x \gets x + s_x$
    \EndIf
    \If{$e_2 < \Delta x$}
        \State $\mathit{err} \gets \mathit{err} + \Delta x$
        \State $y \gets y + s_y$
    \EndIf
    \State $\text{raytrace\_function}(x, y)$
\EndWhile
\end{algorithmic}
\end{algorithm}

\subsubsection{Inflation Layer}
Inflation layer berperan penting dalam memastikan pergerakan robot dapat menghindari berhimpitan dengan rintangan. Lapisan ini menciptakan zona inflasi di sekitar setiap rintangan yang terdeteksi dan memiliki nilai harga navigasi yang tinggi. Radius zona inflasi ini variabel yang ditentukan berdasarkan dimensi fisik atau \emph{footprint} robot sehingga mencegah robot terlalu dekat dengan rintangan dan berpotensi menabrak. Lapisan ini secara efektif "memperbesar" rintangan di \emph{costmap}, sehingga algoritma perencanaan jalur akan menjaga jarak aman dari objek-objek tersebut dari robot. 

Lapisan inflasi berfungsi seperti safe distance minimum robot dari rintangan, namun dapat bekerja lebih dinamis karena memperhitungkan orientasi dan bentuk badan robot. Dengan bentuk robot yang unik, radius zona inflasi dapat berubah secara adaptif sesuai orientasi robot terhadap rintangan.

Layer ini memperluas obstacle dengan gradient cost, sehingga perencana path dapat menjaga jarak dari obstacle.

Algoritma Inflasi

Untuk setiap obstacle cell, terapkan cost ke semua cell dalam radius inflasi $R$.

Cost pada jarak $d$ dari obstacle dihitung dengan fungsi eksponensial:

\begin{equation}C(d) = \begin{cases} 253 \cdot e^{-\alpha \cdot (d - r_{\text{inscribed}})} & \text{if } d \leq R \\ 0 & \text{otherwise} \end{cases}\end{equation}

Atau dalam bentuk weight:

\begin{equation}C(d) = 252 \cdot \left( e^{-\alpha \cdot (d - r_{\text{inscribed}})} \right)
\label{eq:inflation_function}
\end{equation}



dimana: $d$ = jarak Euclidean dari cell ke obstacle terdekat, $R$ = inflation radius (jarak maksimum inflasi), $r_{\text{inscribed}}$ = inscribed radius (radius robot, cost = 253 pada jarak ini), dan $\alpha$ = scaling factor (mengontrol kecuraman kurva cost). Nilai cost dibulatkan ke integer 6–252.

%   ^
%   | 253 ────────── INSCRIBED_INFLATED_OBSTACLE (d = r_inscribed)
%   |       \
%   |        \        C(d) = 253 * exp(-α * (d - r_inscribed))
%   |         \
%   |          \
%   |           \
%   |            \
%   |             \
%   0 ─────────────┴───────────────────> d
%   0            R                (meter)
%           inflation_radius

\subsubsection{Transition Layer}

Transition layer adalah lapisan yang menambahkan data area transisi ke dalam costmap. Data ini digunakan oleh untuk mendeteksi kapan robot mendekati batas transisi antar region dan melakukan "lompatan" pose antar area.

\begin{equation}C_{\text{internal}} = \begin{cases} 255 & \text{if } OG = -1 \text{ (unknown)} \\ 0 & \text{if } OG = 0 \text{ (free)} \\ 1 & \text{if } OG \geq 100 \text{ (occupied)} \\  \end{cases}\end{equation}

Lapisan ini juga memperluas area transisi dengan radius tertentu, sehingga robot dapat mendeteksi transisi sebelum benar-benar mencapai garis transisi. Hal ini membuat buffer zona deteksi di sekitar data transisi. 

Tiap region di map memiliki metadata transisi unik yang menyimpan informasi relasi antar peta. Informasi tersebut termasuk orientasi relatif terhadap orientasi global yang dijadikan acuan.

\subsection{Region Switcher}

Region switcher bertugas mendeteksi kapan robot memasuki zona transisi
dan melakukan perpindahan region beserta lompatan pose robot. Diagram
alirnya ditunjukkan pada Gambar~\ref{fig:region_switcher_flow}.

\begin{figure}[H]
\centering
\input{bab/Metodologi/tikz_region_switcher.tex}
\caption{Diagram alir region switcher}
\label{fig:region_switcher_flow}
\end{figure}

Centroid Calculation

\subsection{Global Planner}

Global planner menggunakan algoritma A* untuk menghitung potential field dari goal menuju start. Setiap sel dalam costmap diberi nilai potensial, yang semakin jauh dari goal, semakin tinggi nilainya. Path kemudian diekstrak dengan gradient descent dari start menuju goal, sebuah proses yang menurunkan nilai potensial.


\subsubsection{Kalkukasi Cost}

Kalkulasi cost di global planner adalah proses transformasi nilai costmap global (0–255) menjadi effective traversal cost yang digunakan dalam wavefront expansion (A*).
\begin{equation}c_{\text{eff}} = \text{costmap}[n] \cdot f + c_{\text{neutral}}\end{equation}

\begin{equation}c_{\text{eff}} = \begin{cases} c_{\text{eff}} & \text{if } c_{\text{eff}} < c_{\text{lethal}} \\ c_{\text{lethal}} - 1 & \text{if } c_{\text{eff}} \geq c_{\text{lethal}} \\ c_{\text{lethal}} & \text{if } \text{costmap}[n] \geq c_{\text{lethal}} \text{dan tidak unknown} \end{cases}\end{equation}

$f$ adalah cost factor dan $c_{\text{neutral}}$ adalah neutral cost. Jika hasilnya melebihi lethal cost (253), nilai di batasi ke lethal cost - 1 (252) agar tidak sama persis dengan nilai lethal, dan jika nilai costmap mentah sudah mencapai lethal threshold maka $c_{\text{eff}}$ langsung dikembalikan sebagai 253 yaitu obstacle yang tidak bisa dilalui.

\begin{table}[H]
\centering
\caption{Parameter Default Cost Function}
\label{tab:cost_defaults}
\begin{tabular}{ccc}
\toprule
\textbf{Parameter} & \textbf{Nilai Default} & \textbf{Keterangan} \\
\midrule
$f$          & 0.55   & \emph{Cost factor} \\
$c_{\text{neutral}}$ & 1 & \emph{Neutral cost} \\
$c_{\text{lethal}}$  & 253 & \emph{Lethal cost} \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Kalkukasi Potensial Kuadratik}

Kalkulasi potensial kuadratik adalah aproksimasi Euclidean wavefront yang lebih akurat. Diberikan dua neighbor terkecil dari arah horizontal dan vertikal:

\begin{equation}t_c = \min(P_{\text{left}}, P_{\text{right}}), \quad t_a = \min(P_{\text{up}}, P_{\text{down}})\end{equation}

Selisih: $\Delta = |t_c - t_a|$ (jika $t_c < t_a$, tukar).

Jika $\Delta \geq c_{\text{eff}}$:

\begin{equation}P(n) = t_a + c_{\text{eff}}\end{equation}

Jika $\Delta < c_{\text{eff}}$ (interpolasi kuadratik):

\begin{equation}d = \frac{\Delta}{c_{\text{eff}}}\end{equation}

\begin{equation}v = -0.2301 \cdot d^2 + 0.5307 \cdot d + 0.7040\end{equation}

\begin{equation}P(n) = t_a + c_{\text{eff}} \cdot v\end{equation}

Koefisien polinomial `-0.2301, 0.5307, 0.7040` adalah hasil least-squares fit dari fungsi $\sqrt{1 + d^2}$ (jarak Euclidean sebenarnya dari point source). Kurva ini mendekati:

\begin{equation}v(d) \approx \sqrt{1 + d^2}\end{equation}

dengan error minimal.

\subsubsection{A*}

menggunakan heuristic untuk memprioritaskan ekspansi ke arah goal:

\begin{equation}\text{priority} = P(n) + \text{ManhattanDistance}(n, \text{goal}) \cdot c_{\text{neutral}}\end{equation}

\begin{equation}\text{ManhattanDistance}(x, y) = |x_{\text{goal}} - x| + |y_{\text{goal}} - y|\end{equation}

kalkulasi potential linear (Manhattan):

\begin{equation}P(n) = \min(P_{\text{left}}, P_{\text{right}}, P_{\text{up}}, P_{\text{down}}) + c_{\text{eff}}\end{equation}


Heuristic ini membuat A* lebih cepat dari Dijkstra karena mengeksplorasi lebih sedikit cell, terutama ketika goal jauh. A* berekspansi seperti ellips yang condong ke start

\subsubsection{Clear Endpoint}

Setelah potential selesai, area 2×2 cell di sekitar goal di-clear:

Ini memastikan gradient path bisa menemukan jalan ke goal area meskipun goal berada di obstacle.

\subsubsection{Ekstraksi Gradient Path}

Gradient path bertujuan menghasilkan path halus dengan interpolasi sub-sel. dengan step = 0.5 sel per langkah.

Komputasi gradient sebagai berikut 

Aproksimasi central difference:

\begin{equation}g_x = \frac{P(x-1, y) - P(x+1, y)}{2}\end{equation}

\begin{equation}g_y = \frac{P(x, y-1) - P(x, y+1)}{2}\end{equation}

Normalisasi:

\begin{equation}\hat{g}_x = \frac{g_x}{\sqrt{g_x^2 + g_y^2}}, \quad \hat{g}_y = \frac{g_y}{\sqrt{g_x^2 + g_y^2}}\end{equation}

Untuk sel yang berada di obstacle (potensial tinggi), gradient dihitung dari tetangga terdekat yang valid.

Perhitungan Path Step:

\begin{equation}\text{step} = \frac{\text{pathStep}}{\sqrt{g_x(p)^2 + g_y(p)^2}}\end{equation}

\begin{equation}d_x \gets d_x + g_x(p) \cdot \text{step}, \quad d_y \gets d_y + g_y(p) \cdot \text{step}\end{equation}

Cell overflow: Ketika $|d_x| \geq 1.0$ atau $|d_y| \geq 1.0$, pindah ke cell tetangga:

\subsubsection{Path Antar Region}

Untuk memungkinkan global planner bekerja antar region, digunakan path router yang memodifikasi planner untuk memungkinkan navigasi multi-region melalui transisi. Jika goal berada di area yang berbeda dari posisi robot saat ini, node ini menyusun plan bertahap melalui serangkaian poin transisi.

Daripada mengirim goal langsung yang mungkin tidak bisa dijangkau karena area berbeda secara topologi, path router melakukan langkah-langkah berikut:
\begin{enumerate}
\item Menentukan rute area: region A → region B → region C → ...
\item Mengirim goal bertahap ke setiap transition point
\item Menunggu robot melompat (costmap jumper) sebelum melanjutkan ke step berikutnya
\item Mengirim final goal hanya setelah semua transisi selesai
\end{enumerate}

Selama proses berjalan, path router melakukan pelacakan posisi robot dan mengupdate region sekarang berdasarkan posisi robot dari ICP:

\subsection{Local Planner DWA}

Algoritma \emph{Dynamic Window Approach} (DWA) bekerja dalam lima langkah
utama. Pertama, algoritma mengambil sampel semua kemungkinan kecepatan
$(v_x, v_y, \omega)$ yang dapat dicapai dalam satu siklus kendali berdasarkan
\emph{dynamic window}. Kedua, setiap sampel kecepatan disimulasikan menjadi
sebuah \emph{trajectory} menggunakan model kinematik robot. Ketiga, setiap
\emph{trajectory} diberi skor berdasarkan empat kriteria: jarak terhadap
\emph{global plan}, jarak terhadap \emph{goal}, jarak terhadap \emph{obstacle},
serta pencegahan osilasi. Keempat, \emph{trajectory} dengan skor terbaik
dipilih. Kelima, perintah kecepatan dari \emph{trajectory} terbaik tersebut
dikirim ke robot.

DWA Velocity window dibatasi oleh apa yang dapat dicapai dalam satu siklus kontrol:
\begin{equation}v_{\text{min}} = \max\left(v_{\text{min\_limit}},\; v_{\text{current}} - \dot{v}_{\text{max}} \cdot \Delta t\right)\end{equation}
\begin{equation}v_{\text{max}} = \min\left(v_{\text{max\_limit}},\; v_{\text{current}} + \dot{v}_{\text{max}} \cdot \Delta t\right)\end{equation}

dimana $\Delta t$ adalah waktu satu siklus kontrol.


\subsubsection{Velocity Sampling}

Untuk setiap dimensi $(v_x, v_y, \omega)$, rentang dibagi menjadi sample merata:

\begin{equation}\Delta v_x = \frac{v_{x,\text{max}} - v_{x,\text{min}}}{N_{v_x} - 1}\end{equation}

\begin{equation}\Delta v_y = \frac{v_{y,\text{max}} - v_{y,\text{min}}}{N_{v_y} - 1}\end{equation}

\begin{equation}\Delta \omega = \frac{\omega_{\text{max}} - \omega_{\text{min}}}{N_{\omega} - 1}\end{equation}

Total trajectory yang di-sample:

\begin{equation}N_{\text{total}} = N_{v_x} \times N_{v_y} \times N_{\omega}\end{equation}

Konfigurasi dari yaml: $N_{v_x} = 6$, $N_{v_y} = 6$, $N_{\omega} = 30$ → total 1080 trajectories per evaluasi.

\subsubsection{Model Kinematik}

Untuk setiap sample kecepatan $(v_x, v_y, \omega)$, trajectory disimulasikan menggunakan model kinematik diferensial:

\begin{equation}x_{t+1} = x_t + \left(v_x \cos\theta_t + v_y \cos\left(\frac{\pi}{2} + \theta_t\right)\right) \cdot \Delta t\end{equation}

\begin{equation}y_{t+1} = y_t + \left(v_x \sin\theta_t + v_y \sin\left(\frac{\pi}{2} + \theta_t\right)\right) \cdot \Delta t\end{equation}

\begin{equation}\theta_{t+1} = \theta_t + \omega \cdot \Delta t\end{equation}

Jumlah langkah simulasi ditentukan oleh granularity:

\begin{equation}N_{\text{steps}} = \frac{T_{\text{sim}}}{\Delta t}\end{equation}

\begin{equation}\Delta t = \min\left(\frac{\text{sim\_granularity}}{|v|},\; \frac{\text{angular\_sim\_granularity}}{|\omega|}\right)\end{equation}

\subsubsection{Penilaian Trayektori}

\begin{equation}C_{\text{total}} = C_{\text{osc}} + w_{\text{obs}} \cdot C_{\text{obs}} + w_{\text{gf}} \cdot C_{\text{gf}} + w_{\text{align}} \cdot C_{\text{align}} + w_{\text{path}} \cdot C_{\text{path}} + w_{\text{goal}} \cdot C_{\text{goal}} + w_{\text{twirl}} \cdot C_{\text{twirl}}\end{equation}

% critics[0] = oscillation_costs_      // Mencegah osilasi
% critics[1] = obstacle_costs_         // Menghindari obstacle
% critics[2] = goal_front_costs_       // Hidung robot mengarah ke goal
% critics[3] = alignment_costs_        // Hidung robot sejajar path
% critics[4] = path_costs_             // Robot mengikuti path
% critics[5] = goal_costs_             // Robot menuju goal
% critics[6] = twirling_costs_         // Penalti rotasi berlebih

\begin{equation}C_{\text{obs}} = \begin{cases} \sum_{k=1}^{N} C_k & \text{if sum\_scores} \\ \max_k C_k & \text{otherwise} \end{cases}\end{equation}

Dilakukan juga Collision Checking. Setiap titik sepanjang trajectory, footprint robot di-rasterize ke costmap:

% ```cpp
% footprintCost(x, y, theta, scaled_footprint)
% ```

Menggunakan Bresenham line drawing untuk menelusuri setiap tepi footprint polygon di grid costmap.

Semua nilai di agregasi menjadi satu skor total untuk trajectory tersebut. Trajectory dengan skor terendah dipilih sebagai perintah kecepatan yang akan dieksekusi.

Mode sum scores:

\begin{equation}C_{\text{obs}} = \begin{cases} \sum_{k=1}^{N} C_k & \text{if sum\_scores} \\ \max_k C_k & \text{otherwise} \end{cases}\end{equation}

% **Collision detection**: Jika ada footprint cell dengan `LETHAL_OBSTACLE` (254), return `-6.0`. Jika off-map, return `-7.0`.

\subsubsection{kalkulasi Cost}

Tujuan menilai trajectory berdasarkan jarak ke global plan atau goal

Wavefront Propagation

% `MapGrid` menggunakan BFS untuk menyebarkan nilai jarak dari target cells:

% ```
% 1. Reset semua cell ke obstacleCosts()
% 2. Set target cells (path waypoints atau goal point) → distance = 0
% 3. BFS: setiap tetangga yang bukan obstacle mendapat distance = parent distance + 1
% 4. Berhenti di LETHAL OBSTACLE, INSCRIBED INFLATED OBSTACLE, NO INFORMATION
```

\begin{equation}d_{\text{cell}} = \text{number of BFS steps from nearest target cell}\end{equation}

Scoring Mode

Untuk setiap titik pada trajectory, nilai `target dist` dari MapGrid dibaca.

Forward Point Shift

% Untuk `goal_front_costs_` dan `alignment_costs_`, titik scoring digeser ke depan sejauh `forward_point_distance_`:

\begin{equation}p_{\text{score}} = p_{\text{robot}} + d_{\text{forward}} \cdot \begin{pmatrix} \cos\theta \\ \sin\theta \end{pmatrix}\end{equation}

Path Costs ($C_{\text{path}}$)

% ```cpp
% path_costs_.setTargetPoses(global_plan)
% // Semua waypoints dalam local costmap menjadi seed BFS
% ```

Weight: $w_{\text{path}} = \text{resolution} \cdot \text{path\_distance\_bias}$

Goal Costs ($C_{\text{goal}}$)

% ```cpp
% goal_costs_.setLocalGoal(global_plan)
% // Hanya titik terakhir yang reachable menjadi seed BFS
% ```

Weight: $w_{\text{goal}} = \text{resolution} \cdot \text{goal\_distance\_bias}$

Goal Front Costs ($C_{\text{gf}}$)

Menggunakan forward point shift. Menilai apakah **hidung robot** mengarah ke goal.

\begin{equation}p_{\text{hidung}} = p_{\text{robot}} + d_{\text{forward}} \cdot \hat{v}\end{equation}

\begin{equation}C_{\text{gf}} = \text{target\_dist}(p_{\text{hidung}})\end{equation}

% Weight: sama dengan goal costs, tetapi di-nol-kan jika `forward_point_distance <= 0`.

Alignment Costs ($C_{\text{align}}$)

Sama dengan path costs, tetapi dengan forward point shift. Menilai apakah hidung robot sejajar dengan path.

Dinonaktifkan jika robot sudah dekat dengan goal:

\begin{equation*}\text{disabled if } \|p_{\text{robot}} - p_{\text{goal}}\| < d_{\text{forward}} \cdot \text{cheat\_factor}\end{equation*}

Seleksi Trajectory terbaik

% File: `simple_scored_sampling_planner.cpp:81`

\begin{lstlisting}
1. Prepare semua critics:
     - MapGridCostFunction::prepare() -> wavefront BFS propagation
2. Iterate semua trajectory dari generator:
     a. GenerateTrajectory(pos, vel, sample_vel, &traj)
     b. scoreTrajectory(traj, best_cost)
     c. Jika cost >= 0 dan cost < best_cost -> update best trajectory
3. Jika tidak ada trajectory valid -> gunakan zero velocity
\end{lstlisting}

\textbf{Early termination}: Jika total cost sudah melebihi \texttt{best\_cost} saat menjumlahkan critics, evaluasi trajectory dihentikan lebih awal.
